# NeuralOperator progress update — 20 September 2026

This note supersedes the status of the execution protocol in the main roadmap
for the current main baseline.

## Verified baseline

- main contains the spectral oracle, Burgers reference solver and dataset
  provenance, a minimal NumPy/SciPy FNO, a periodic interpolation and
  convolution baseline, and a separable linear DeepONet contract.
- All pull requests currently recorded for the repository are merged.
- The latest observed GitHub Actions test runs on main were successful.
- The repository remains a research reference implementation: it now contains an
  optional PyTorch autodiff backend, but not a nonlinear production DeepONet, a
  Rust kernel, or a validated performance advantage over a classical solver.

## Added in this tranche

The rollout evaluator makes the operator contract and the recursive test
explicit:

1. rollout_burgers_operator applies a one-transition operator repeatedly and
   rejects shape, type and non-finite-value violations.
2. evaluate_burgers_rollout reports per-step MSE and relative L2 error,
   finite-horizon transition residual, and mean/energy mismatch against the
   reference trajectory.
3. scripts/burgers_rollout_experiment.py trains the existing reference FNO
   and emits a JSON stability report over five recursive transitions.
4. Unit tests validate an exact solver-backed operator and invalid output
   handling.

The transition residual is intentionally documented as a discrete diagnostic;
it is not a continuous-time PINO objective.

## Optional autodiff backend

The current branch adds `TorchFNO1D` with the same
`(batch, points, channels)` contract as the NumPy/SciPy FNO. Its full-batch
training loop uses PyTorch automatic differentiation and Adam; the base
requirements remain NumPy/SciPy only, and the optional tests are skipped when
PyTorch is unavailable. The implementation is intentionally not reported as a
speedup or as numerical parity until it is run on the same deterministic
train/validation/OOD split with multiple seeds.

The merged Burgers protocol now also carries static sinusoidal forcing
provenance in the solver, dataset, physics diagnostic, and OOD evaluator.

## Next execution order

1. Run the merged train/validation/OOD split, including static-forcing cases,
   and archive its rollout metrics.
2. Compare the optional autodiff backend with the NumPy oracle on that same split
   using multiple seeds and explicit cost/error reporting.
3. Add a genuinely nonlinear DeepONet and compare it with FNO, interpolation and
   local convolution using the same split and rollout metrics.
4. Add Darcy 2D and advection-diffusion truth solvers before any broad claim
   about resolution transfer.
5. Port only the numerically accepted kernels to SciRust/NNIS and retain
   cross-backend tolerance tests.
6. Keep the merged PolyForm licensing and copyright notice synchronized with
   future files and derivative repositories.


## Current checkpoint

- PR #11 through PR #15 are merged on main.
- The latest main CI run for the licensing change completed successfully.
- There are no open issues or unmerged pull requests currently recorded for the repository.

The next implementation gate is an optional autodiff backend with the existing
NumPy/SciPy implementation retained as the acceptance oracle. No speedup or
production readiness claim is made until that comparison is measured.
